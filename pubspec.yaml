name: witt
description: Simple state management powered by ValueNotifier with service locator.
version: 1.1.0
repository: https://github.com/weaze-id/witt

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  collection: ^1.16.0
  flutter:
    sdk: flutter
  nested: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
