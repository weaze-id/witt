name: example_app
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  witt:
    path: "../"

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
